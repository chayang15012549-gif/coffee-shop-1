# Deluxe Cafe - AI Integration Setup Guide

## วิธีติดตั้ง OpenAI AI Features

### ขั้นตอนที่ 1: สร้าง OpenAI Account
1. ไปที่ https://platform.openai.com
2. สมัครสมาชิก และ Login
3. ไปที่ https://platform.openai.com/api-keys
4. คลิก "Create new secret key" เพื่อสร้าง API Key
5. คัดลอก API Key (บันทึกไว้อย่างปลอดภัย)

### ขั้นตอนที่ 2: ติดตั้ง Dependencies
```bash
pip install -r requirements.txt
```

### ขั้นตอนที่ 3: ตั้งค่า Environment Variable
#### วิธี 1: ใช้ .env file (แนะนำ)
1. ไปที่ folder root ของโปรเจค
2. สร้างไฟล์ `.env`
3. เพิ่มบรรทัดต่อไปนี้:
```
OPENAI_API_KEY=sk-your-api-key-here
```

#### วิธี 2: ตั้ง System Environment Variable
- **Windows**: 
  ```
  setx OPENAI_API_KEY "sk-your-api-key-here"
  ```
- **Mac/Linux**:
  ```
  export OPENAI_API_KEY="sk-your-api-key-here"
  ```

### ขั้นตอนที่ 4: รัน Application
```bash
python app.py
```

## ฟีเจอร์ที่เพิ่มเข้ามา

### AI Product Description Generator
- **ที่ไหน**: Admin Dashboard → Add Product Section
- **วิธีใช้**: 
  1. กรอกชื่อสินค้า (บังคับ)
  2. กรอกราคา (ถ้าอยากได้)
  3. คลิกปุ่ม "🎇 AI" เพื่อสร้างรายละเอียดอัตโนมัติ
  4. ระบบจะสร้างรายละเอียดของสินค้าด้วย ChatGPT
  5. ตรวจสอบและแก้ไขได้ถ้าต้องการ
  6. กด "เพิ่มสินค้า" เพื่อบันทึก

### API Endpoint
- **POST** `/api/generate-description`
  - ต้องการ login ก่อน
  - Request body:
    ```json
    {
      "name": "ชื่อสินค้า",
      "price": 350.00
    }
    ```
  - Response:
    ```json
    {
      "success": true,
      "description": "รายละเอียดที่สร้างด้วย AI"
    }
    ```

## หมายเหตุผู้ใช้งาน

### ข้อเสีย และประเด็นที่ควรทราบ:
- ต้องมี OpenAI API Key (อาจมีค่าใช้ "Pay-as-you-go")
- ขึ้นอยู่กับการเชื่อมต่ออินเทอร์เน็ต
- Rate limit: OpenAI มีข้อจำกัดของคำขอต่อนาที
- ถ้าไม่มี API Key การสร้างรายละเอียดจะใช้ default fallback

### การจัดการ API Key อย่างปลอดภัย:
- ✅ เก็บ API Key ในไฟล์ `.env` (ไม่ commit ขึ้น git)
- ✅ เพิ่ม `.env` ลงใน `.gitignore`
- ❌ อย่าเพิ่ม API Key ลงใน Code โดยตรง
- ❌ อย่า Share API Key กับใครสักคน

## Troubleshooting

### ปัญหา: "ModuleNotFoundError: No module named 'openai'"
**วิธีแก้**: ติดตั้ง openai package
```bash
pip install openai
```

### ปัญหา: "API key not found"
**วิธีแก้**: ตรวจสอบว่ากำหนด OPENAI_API_KEY ถูกต้อง
1. ตรวจสอบไฟล์ `.env`
2. หรือตั้ง environment variable
3. + Restart Flask app

### ปัญหา: "Invalid API key provided"
**วิธีแก้**: 
- ตรวจสอบว่า API Key ถูกต้อง
- ไม่มี space หรือ character พิเศษ
- ยังไม่หมดอายุ

## สำหรับผู้พัฒนา

### ไฟล์ที่แก้ไข:
1. `app.py` - เพิ่ม OpenAI integration และ API endpoint
2. `templates/admin.html` - เพิ่ม UI สำหรับ AI generation
3. `requirements.txt` - ใหม่ (dependencies)
4. `.env.example` - ไฟล์ตัวอย่าง

### Code Structure:
```python
# การสร้างรายละเอียด
def generate_product_description(product_name, product_price):
    # สร้าง prompt ให้ ChatGPT
    # ส่ง request ไป OpenAI
    # ส่งกลับผลลัพธ์

# API Endpoint สำหรับ admin panel
@app.route('/api/generate-description', methods=['POST'])
def generate_description():
    # ตรวจสอบ login
    # รับ name และ price
    # เรียก generate_product_description()
    # ส่งกลับ JSON response
```

## ข้อมูลเพิ่มเติม
- [OpenAI API Documentation](https://platform.openai.com/docs)
- [OpenAI Pricing](https://openai.com/pricing)
- [Python-dotenv Documentation](https://python-dotenv.readthedocs.io/)
